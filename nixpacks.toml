[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "xvfb-run", "tk", "tcl"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 180"
