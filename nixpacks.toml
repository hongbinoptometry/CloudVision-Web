[phases.setup]
aptPkgs = ["xvfb", "python3-tk", "ffmpeg", "libgl1", "libglib2.0-0", "fonts-noto-cjk"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "CLOUD_MODE=1 xvfb-run -a python app.py"
