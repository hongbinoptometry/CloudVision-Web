[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "xorg.xvfb", "tk", "zlib", "libjpeg", "freetype"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "python -m pip install -r requirements.txt"]

[start]
cmd = "CLOUDVISION_CLOUD=1 xvfb-run -a python app.py"
