[phases.setup]
nixPkgs = ["python311", "xorg.xorgserver", "tk", "zlib", "libjpeg", "freetype"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "CLOUD_DEPLOY=1 xvfb-run -a python app.py"
